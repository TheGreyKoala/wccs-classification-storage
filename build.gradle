group = "de.koalaworks.wccs"
version "1.0.0"

apply plugin: "distribution"
apply plugin: "maven-publish"

distributions {
    main {
        contents {
            from "Dockerfile", "custom-entrypoint.sh"
        }
    }
}

publishing {
    publications {
        tar(MavenPublication ) {
            artifact distTar
        }
    }
}
